{
  "name": "auto_tactics",
  "devDependencies": {
    "@types/bun": "^1.1.4"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "scripts": {
    "build": "bun build --outdir static --sourcemap=external --splitting frontend/index.ts",
    "watch": "bun run build --watch"
  }
}
